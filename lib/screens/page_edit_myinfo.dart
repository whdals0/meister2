import 'package:flutter/material.dart';

class PageEditMyInfo extends StatefulWidget {
  const PageEditMyInfo({Key? key}) : super(key: key);

  @override
  State<PageEditMyInfo> createState() => _PageEditMyInfoState();
}

class _PageEditMyInfoState extends State<PageEditMyInfo> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: null,
      body: Container(
        color: Colors.white,
      ),
    );
  }
}
